## 1. Style Adjustments

- [x] 1.1 Move the `background-color` property from the `body` selector to the `html` selector in `styles.css`.
- [x] 1.2 Update the `top` and `left` offset values in the `body::before` block in `styles.css` to `top: -50vh` and `left: -50vw`.

## 2. Validation

- [x] 2.1 Verify that the background image renders visibly and is centered correctly in the viewport.
- [x] 2.2 Run OpenSpec validate to confirm compliance.
