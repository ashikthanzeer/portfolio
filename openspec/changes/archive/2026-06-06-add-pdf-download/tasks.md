## 1. PDF Asset Generation

- [x] 1.1 Trigger headless Google Chrome binary to convert `resume.html` into a printable A4 `resume.pdf` file.

## 2. Main Site updates

- [x] 2.1 Add the "Download PDF" action link/button to the header-actions section of `index.html`.
- [x] 2.2 Style the new "Download PDF" button in `styles.css` with cyberpunk glowing borders.

## 3. Resume Page updates

- [x] 3.1 Update `resume.html` header layout to include a "Download PDF" action link/button next to the "Back to Portfolio" link.

## 4. Validation

- [x] 4.1 Verify that both links download the `resume.pdf` file correctly and inspect the A4 format.
- [x] 4.2 Run OpenSpec validate to ensure change document compliance.
