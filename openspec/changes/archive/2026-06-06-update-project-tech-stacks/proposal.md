## Why

The technical stacks listed for the user's projects are incorrect. Specifically, "StudyPlanner" is built with React, Vite, TypeScript, Express.js, and PostgreSQL, while "ScoreFusion" and "Expense Tracker" are built using standard HTML, JavaScript, and CSS (without Python or MySQL backends). Correcting these details is necessary for professional accuracy on both the portfolio and the resume.

## What Changes

This change:
- Modifies the project display cards in `index.html` to reflect the updated tech stack tags (badges) for all three projects.
- Modifies the project sections and text listings in `resume.html` to update the tech stack sub-headings.
- Corrects any other mention of Python/MySQL from these projects on both the site and the resume pages.

## Capabilities

### New Capabilities

<!-- None -->

### Modified Capabilities

- `projects-showcase`: Update tech stack tags and badges for StudyPlanner, ScoreFusion, and Expense Tracker in the projects section.
- `resume-viewer`: Update tech stack headings in the projects list of the resume page.

## Impact

Modifies `index.html` and `resume.html` to update the projects list text and tags.
