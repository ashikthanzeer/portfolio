## 1. Asset Preparation

- [x] 1.1 Copy the background video `C:\Users\ashik_rqf6ipg\Downloads\12118424_1920_1080_30fps.mp4` to the project root directory as `bg.mp4`
- [x] 1.2 Delete the existing background image `bg.jpg` from the project directory

## 2. Code Implementation

- [x] 2.1 Add the `<video>` container and background video element into `index.html`
- [x] 2.2 Update background styles in `styles.css` to remove the static background and position the new video background element behind all other layout content

## 3. Verification

- [x] 3.1 Verify that the background video plays automatically, is muted, loop-plays, and overlays correctly without obscuring content or interactive elements
