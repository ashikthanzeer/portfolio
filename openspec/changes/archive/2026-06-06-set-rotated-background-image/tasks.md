## 1. Asset Configuration

- [x] 1.1 Copy the user's background paint texture image to the project root directory as `bg.jpg`.

## 2. Style Integration

- [x] 2.1 Add the rotated pseudo-element background style under `body::before` in `styles.css`.
- [x] 2.2 Tweak the background opacity and text color compatibility to guarantee legibility.

## 3. Validation

- [x] 3.1 Verify visual rendering and response across standard window sizes.
- [x] 3.2 Run OpenSpec validate to confirm compliance.
