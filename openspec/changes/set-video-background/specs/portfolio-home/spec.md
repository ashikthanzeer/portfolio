## MODIFIED Requirements

### Requirement: Aesthetics and Dark Theme Styling
The portfolio page SHALL render with a high-tech, futuristic dark theme featuring a visible, fixed background video (`bg.mp4`), neon cyan/amber accents, glowing interactive elements, and responsive layout.

#### Scenario: Styling Verification
- **WHEN** the landing page is rendered
- **THEN** it MUST display the background video (`bg.mp4`) as a loop-playing, muted, and inline-playing background layer behind the content grid, without being hidden by body solid color overrides.
